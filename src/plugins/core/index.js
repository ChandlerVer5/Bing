import autocomplete from './autocomplete'
import quit from './quit'
import plugins from './plugins'
import settings from './settings'
import version from './version'
import reload from './reload'

export default {
  autocomplete,
  quit,
  plugins,
  settings,
  version,
  reload
}

// TODO:版本应该不需要了，应该还缺少 词典和网络搜索
